import { Observable } from 'rxjs/Observable';
import { ActivatedRoute } from '@angular/router';
export declare class PreviewContainerComponent {
    caseID$: Observable<string>;
    constructor(route: ActivatedRoute);
}
